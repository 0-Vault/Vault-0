{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for main window.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "pty:default",
    "shell:allow-open"
  ]
}
